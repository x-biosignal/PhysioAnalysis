% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{bandPower}
\alias{bandPower}
\title{Compute band power}
\usage{
bandPower(x, bands = NULL, method = c("welch", "wavelet"), relative = FALSE)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{bands}{Named list of frequency bands. Each element should be c(low, high).
Default includes standard EEG bands.}

\item{method}{Method: "welch" (PSD) or "wavelet".}

\item{relative}{If TRUE, returns relative power (proportion of total).}
}
\value{
A \code{data.frame} with one row per channel and columns for the channel
name and power in each frequency band. If \code{relative = TRUE}, values
represent the proportion of total power.
}
\description{
Extracts power in specified frequency bands.
}
\examples{
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(2560), nrow = 256, ncol = 10)),
  colData = S4Vectors::DataFrame(label = paste0("Ch", 1:10)),
  samplingRate = 256
)

# Compute band power for standard EEG bands
bp <- bandPower(pe)
head(bp)

# Compute relative band power
bp_rel <- bandPower(pe, relative = TRUE)
}
\references{
Oppenheim, A.V. & Willsky, A.S. (1997). "Signals and Systems."
2nd ed. Prentice Hall.
}
\seealso{
\code{\link[=spectrogram]{spectrogram()}} for full time-frequency decomposition,
\code{\link[=waveletTransform]{waveletTransform()}} for wavelet-based power, \code{\link[=fftSignals]{fftSignals()}} for raw FFT,
\code{\link[=plotPSD]{plotPSD()}} for power spectral density visualization.
}
