% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{plotSpectrogram}
\alias{plotSpectrogram}
\title{Plot spectrogram}
\usage{
plotSpectrogram(spec, freq_range = NULL, log_power = TRUE)
}
\arguments{
\item{spec}{Spectrogram result from spectrogram().}

\item{freq_range}{Optional frequency range to display.}

\item{log_power}{If TRUE, displays log power (dB scale).}
}
\value{
A \code{ggplot} object showing the spectrogram as a filled raster plot
with time on the x-axis and frequency on the y-axis.
}
\description{
Creates a visualization of a spectrogram result.
}
\examples{
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(1000 * 4), nrow = 1000)),
  samplingRate = 250
)

# Compute spectrogram
spec <- spectrogram(pe, channel = 1)

# Plot with frequency range filter
plotSpectrogram(spec, freq_range = c(1, 50))
}
\references{
Oppenheim, A.V. & Willsky, A.S. (1997). "Signals and Systems."
2nd ed. Prentice Hall.
}
\seealso{
\code{\link[=spectrogram]{spectrogram()}} to compute the spectrogram data, \code{\link[=plotPSD]{plotPSD()}} for
power spectral density plots, \code{\link[=plotSignal]{plotSignal()}} for time-domain visualization.
}
