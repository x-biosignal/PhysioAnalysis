% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{linearRegression}
\alias{linearRegression}
\title{Simple linear regression (ordinary least squares)}
\usage{
linearRegression(x, y = NULL)
}
\arguments{
\item{x}{Either the numeric predictor vector (with \code{y} the response), or a
two-column matrix/data frame (column 1 = \code{x}, column 2 = \code{y}), or a list of
two numeric vectors. Incomplete \verb{(x, y)} pairs are dropped.}

\item{y}{The numeric response vector when \code{x} is a single predictor vector.}
}
\value{
A list with \code{slope}, \code{intercept}, \code{r_squared}, \code{statistic} (the
slope t-statistic), \code{p_value} (two-sided), \code{df} (\code{n - 2}) and \code{n}.
}
\description{
Univariate OLS regression of \code{y} on \code{x}: the slope, intercept, R-squared,
and the two-sided t-test of the slope. The slope, R-squared and p-value
reproduce base R \code{stats::lm} / \code{summary.lm} and \code{scipy.stats.linregress}
bit-for-bit (to machine precision against \code{lm}'s QR path).
}
\examples{
linearRegression(1:20, 2 * (1:20) + rnorm(20))
}
\seealso{
\code{\link{correlationTest}}
}
