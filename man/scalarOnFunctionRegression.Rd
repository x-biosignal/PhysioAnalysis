% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-functional-regression.R
\name{scalarOnFunctionRegression}
\alias{scalarOnFunctionRegression}
\title{Scalar-on-function regression (functional coefficient)}
\usage{
scalarOnFunctionRegression(y, curves, npc = NULL, ve = 0.95)
}
\arguments{
\item{y}{Length-\code{N} numeric outcome.}

\item{curves}{\verb{N x P} matrix of predictor curves.}

\item{npc}{Number of functional principal components (default: enough to
explain \code{ve} variance).}

\item{ve}{Variance-explained target for choosing \code{npc} when \code{npc} is \code{NULL}
(default 0.95).}
}
\value{
a \code{sofr_result}: \code{beta} (length-\code{P} coefficient function), \code{intercept},
\code{fitted}, \code{r_squared}, \code{npc}, \code{scores}, \code{pve} (proportion variance explained
by the retained PCs).
}
\description{
Predicts a scalar outcome from a predictor CURVE (e.g. a clinical score from a
joint-moment waveform). The curve is reduced to functional principal
components, the outcome is regressed on the scores, and the coefficients are
mapped back to a coefficient function beta(t): where along the curve the
predictor matters.
}
\examples{
set.seed(2)
P <- 80; t <- seq(0, 1, length.out = P)
X <- matrix(rnorm(50 * P), 50, P) + outer(rnorm(50), sin(2 * pi * t))
beta_true <- dnorm(t, 0.3, 0.05)
y <- as.numeric(scale(X \%*\% beta_true)) + rnorm(50, 0, 0.3)
m <- scalarOnFunctionRegression(y, X, npc = 5)
m$r_squared
}
\references{
Reiss PT, Ogden RT (2007) scalar-on-function regression; Ramsay &
Silverman (2005).
}
\seealso{
\code{\link[=functionalRegression]{functionalRegression()}}
}
