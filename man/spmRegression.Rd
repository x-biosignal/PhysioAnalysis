% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-spm-regress.R
\name{spmRegression}
\alias{spmRegression}
\title{SPM linear-regression t-field}
\usage{
spmRegression(x, predictor, alpha = 0.05, two_tailed = TRUE)
}
\arguments{
\item{x}{A \code{PhysioExperiment} or a numeric matrix (time x observations).}

\item{predictor}{Numeric covariate, one value per observation (column).}

\item{alpha}{Significance level (default 0.05).}

\item{two_tailed}{Logical; two-tailed slope test (default \code{TRUE}).}
}
\value{
A list of class \code{"spm_result"} (\code{test_type = "regression"})
with the \code{t} field, RFT \code{threshold}, significant \code{clusters},
pointwise \code{p_values}, \code{df}, \code{fwhm}, \code{resel_count}, and
the fitted \code{slope}/\code{intercept} fields.
}
\description{
Computes an SPM\{t\} field testing, at every node, the slope of a linear
regression of the waveform on a continuous predictor. The node-wise statistic
is the ordinary least-squares slope t-value (identical to
\code{summary(lm(y ~ predictor))} at that node), and field-level significance
uses the same random-field-theory threshold as \code{\link[=spmTTest]{spmTTest()}} with
\code{df = n - 2}.
}
\examples{
set.seed(1)
pred <- rnorm(20)
data <- matrix(rnorm(100 * 20), nrow = 100)
data[40:60, ] <- data[40:60, ] + outer(rep(1.5, 21), pred)  # slope 30\% window
spmRegression(data, pred)
}
\references{
Pataky 2016; Friston et al. 2007 (RFT). spm1d.stats.regress.
}
\seealso{
\code{\link[=spmTTest]{spmTTest()}}, \code{\link[=spmMANOVA]{spmMANOVA()}}, \code{\link[=spmSnPM]{spmSnPM()}}
}
