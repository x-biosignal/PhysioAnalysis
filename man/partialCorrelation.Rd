% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{partialCorrelation}
\alias{partialCorrelation}
\title{Partial correlation controlling for covariates}
\usage{
partialCorrelation(x, y = NULL, z = NULL, method = c("pearson", "spearman"))
}
\arguments{
\item{x}{Either the first numeric variable (with \code{y} and \code{z} given), or a
matrix/data frame whose column 1 is \code{x}, column 2 is \code{y} and the remaining
columns are the covariates. Incomplete rows are dropped.}

\item{y}{The second numeric variable when \code{x} is a single vector.}

\item{z}{The covariate(s) to control for: a numeric vector or matrix.}

\item{method}{"pearson" (default) or "spearman" (rank-based).}
}
\value{
A list with \code{statistic} (the partial correlation), \code{p_value}
(two-sided t-test), \code{df} (\code{n - 2 - k}), \code{n} and \code{k} (number of covariates).
}
\description{
The correlation between \code{x} and \code{y} after linearly removing one or more
covariates \code{z} from both -- the standard tool for asking whether an
association survives adjustment for a confound. With \code{method = "spearman"}
the variables are rank-transformed first (a rank / robust partial
correlation). The coefficient reproduces base R's residual method and the
\code{ppcor} package to machine precision.
}
\examples{
x <- rnorm(50); z <- rnorm(50); y <- 0.5 * z + rnorm(50)
partialCorrelation(x, y, z)
}
\seealso{
\code{\link{correlationTest}}, \code{\link{kendallTau}}
}
