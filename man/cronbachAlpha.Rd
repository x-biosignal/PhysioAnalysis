% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{cronbachAlpha}
\alias{cronbachAlpha}
\title{Cronbach's alpha (internal-consistency reliability)}
\usage{
cronbachAlpha(x)
}
\arguments{
\item{x}{A respondents-by-items numeric matrix or data frame (rows =
respondents, columns = scale items). Rows with any missing value are dropped.}
}
\value{
A list with \code{alpha} (raw Cronbach's alpha), \code{average_r} (mean
inter-item correlation), \code{k} (items) and \code{n} (respondents).
}
\description{
Cronbach's alpha, the standard index of a scale's internal-consistency
reliability: how consistently a set of \code{k} items measures a single construct.
Computed from the item variances and the total-score variance. The raw alpha
and the mean inter-item correlation reproduce base R, the \code{psych} package
(\code{psych::alpha} raw_alpha) and \code{pingouin.cronbach_alpha} bit-for-bit.
}
\examples{
cronbachAlpha(matrix(round(runif(150, 1, 5)), ncol = 5))
}
