% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-specparam.R
\name{specparam}
\alias{specparam}
\alias{print.specparam_result}
\title{Spectral parameterization (specparam / FOOOF)}
\usage{
specparam(
  x,
  freq_range = c(1, 45),
  peak_width_limits = c(1, 12),
  max_n_peaks = 6L,
  aperiodic_mode = c("fixed", "knee"),
  min_peak_height = 0.05,
  peak_threshold = 2,
  nperseg = NULL,
  assay_name = NULL
)

\method{print}{specparam_result}(x, ...)
}
\arguments{
\item{x}{A \code{specparam_result}.}

\item{freq_range}{Numeric length-2 frequency range in Hz to fit
(default: \code{c(1, 45)}).}

\item{peak_width_limits}{Numeric length-2 minimum and maximum peak bandwidth
in Hz (default: \code{c(1, 12)}).}

\item{max_n_peaks}{Maximum number of periodic peaks to fit (default: 6).}

\item{aperiodic_mode}{\code{"fixed"} for a pure 1/f (offset, exponent) or
\code{"knee"} to also fit a knee frequency.}

\item{min_peak_height}{Minimum peak height above the aperiodic fit, in
\code{log10} power (default: 0.05).}

\item{peak_threshold}{Minimum peak height in units of the standard deviation
of the flattened spectrum (default: 2).}

\item{nperseg}{Welch segment length in samples (default: about a 2-second
segment for adequate low-frequency resolution).}

\item{assay_name}{Input assay name (default: the default assay).}

\item{...}{Ignored.}
}
\value{
An object of class \code{"specparam_result"}: a list with
\code{aperiodic} (a data.frame of per-channel offset, exponent, and knee),
\code{peaks} (a data.frame of per-channel center frequency CF, power PW, and
bandwidth BW), \code{fit} (per-channel R-squared and error), \code{spectra}
(per-channel log spectra and fitted model for plotting), and the settings
used.
}
\description{
Parameterizes each channel's power spectrum into an aperiodic 1/f component
(offset, exponent, and an optional knee) plus a set of Gaussian periodic
peaks, following the specparam / FOOOF algorithm (Donoghue et al. 2020). The
aperiodic exponent is a marker of excitation/inhibition balance and cortical
state; the peaks capture genuine oscillations separated from the 1/f
background. The fit runs on the Welch PSD computed by the same machinery as
\code{\link[=bandPower]{bandPower()}}.
}
\examples{
\dontrun{
pe <- make_pe_2d(n_time = 4000, n_channels = 4, sr = 250)
sp <- specparam(pe, freq_range = c(1, 40))
sp$aperiodic
}
}
\references{
Donoghue, T., et al. (2020). Parameterizing neural power spectra into
periodic and aperiodic components. Nature Neuroscience, 23(12), 1655-1665.
}
\seealso{
\code{\link[=plotSpecparam]{plotSpecparam()}}, \code{\link[=specparamBiomarker]{specparamBiomarker()}}, \code{\link[=bandPower]{bandPower()}}
}
