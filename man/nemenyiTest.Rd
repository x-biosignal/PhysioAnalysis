% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{nemenyiTest}
\alias{nemenyiTest}
\title{Nemenyi post-hoc test (all pairwise comparisons after a Friedman test)}
\usage{
nemenyiTest(x)
}
\arguments{
\item{x}{A blocks-by-treatments numeric matrix (rows = subjects/blocks,
columns = conditions/treatments); rows with any missing value are dropped.}
}
\value{
A data frame with one row per pair: \code{comparison}, \code{statistic} and
\code{p_adj}, carrying \code{k} (treatments) and \code{n} (complete blocks) as attributes.
}
\description{
The Nemenyi test: all pairwise comparisons of the treatments following a
Friedman repeated-measures test (\code{\link{friedmanTest}}), from the
within-block mean ranks and the studentized-range distribution with
single-step control of the family-wise error rate. It is the repeated-measures
counterpart of Tukey's HSD (\code{\link{tukeyHSD}}) and Dunn's test
(\code{\link{dunnTest}}). The statistics and p-values reproduce base R's
Nemenyi formula, the \code{PMCMRplus} package and
\code{scikit_posthocs.posthoc_nemenyi_friedman} to machine precision.
}
\examples{
m <- matrix(rnorm(40) + rep(c(0, 0.5, 1, 1.5), each = 10), nrow = 10)
colnames(m) <- c("a", "b", "c", "d"); nemenyiTest(m)
}
\seealso{
\code{\link{friedmanTest}}, \code{\link{tukeyHSD}}, \code{\link{dunnTest}}
}
