% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-epoch.R
\name{epochData}
\alias{epochData}
\title{Epoching operations for PhysioExperiment}
\usage{
epochData(
  x,
  tmin = -0.2,
  tmax = 0.8,
  event_type = NULL,
  baseline = NULL,
  reject = NULL,
  events = NULL,
  min_length = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{tmin}{Time before event onset in seconds (negative for pre-stimulus).}

\item{tmax}{Time after event onset in seconds.}

\item{event_type}{Character vector of event types to epoch around.
If NULL, uses all events. Ignored if \code{events} is provided.}

\item{baseline}{Numeric vector of length 2 specifying baseline period
(tmin, tmax) for baseline correction. NULL for no correction.}

\item{reject}{Amplitude threshold for epoch rejection. NULL to keep all.}

\item{events}{An EventQuery object for advanced event filtering.
If provided, overrides \code{event_type}.}

\item{min_length}{Minimum epoch length in seconds when using variable-length
epochs (tmax as event name). Epochs shorter than this are excluded.}
}
\value{
A new \code{PhysioExperiment} object with a 4D assay
(time x channel x epoch x sample) named \code{"epoched"}. Metadata includes
\code{epoch_tmin}, \code{epoch_tmax}, \code{epoch_info} (a DataFrame with epoch_id,
event_type, event_value, event_onset), and \code{n_epochs}.
}
\description{
Functions for segmenting continuous data into epochs/trials based on events.
Epoch data around events
}
\details{
Extracts epochs (segments) of data around specified events.
}
\examples{
# Create continuous data with events
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(1000 * 4), nrow = 1000)),
  samplingRate = 100
)
pe <- addEvents(pe, onset = c(1, 2, 3, 4, 5), type = "stimulus")

# Extract epochs: 200ms before to 800ms after stimulus
epochs <- epochData(pe, tmin = -0.2, tmax = 0.8)

# With baseline correction
epochs_bl <- epochData(pe, tmin = -0.2, tmax = 0.8,
                       baseline = c(-0.2, 0))

# With artifact rejection
epochs_clean <- epochData(pe, tmin = -0.2, tmax = 0.8,
                          reject = 100)
}
\references{
Luck, S.J. (2014). "An Introduction to the Event-Related
Potential Technique." 2nd ed. MIT Press.
}
\seealso{
\code{\link[=averageEpochs]{averageEpochs()}} to average across epochs, \code{\link[=epochTimes]{epochTimes()}} to get
the time vector, \code{\link[=tTestEpochs]{tTestEpochs()}} for statistical testing on epoched data,
\code{\link[=plotERP]{plotERP()}} for ERP visualization.
}
