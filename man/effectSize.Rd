% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-tests.R
\name{effectSize}
\alias{effectSize}
\title{Compute effect size (Cohen's d / Hedges g)}
\usage{
effectSize(
  x,
  condition1 = NULL,
  condition2 = NULL,
  pooled = TRUE,
  correction = c("none", "hedges"),
  conf_level = 0.95
)
}
\arguments{
\item{x}{An epoched PhysioExperiment object (4D data).}

\item{condition1}{Indices for first condition.}

\item{condition2}{Indices for second condition. If NULL, computes d against zero.}

\item{pooled}{If TRUE (default for two-sample), uses pooled standard deviation.}

\item{correction}{\code{"none"} for Cohen's d (default), or \code{"hedges"} to apply the
Hedges bias correction \code{J = 1 - 3/(4*df - 1)} to the estimate and interval.}

\item{conf_level}{Confidence level for the interval (default 0.95).}
}
\value{
A list containing:
\item{d}{Matrix of standardized mean differences (time x channel)}
\item{ci_lower}{Lower confidence limit for d (non-central t)}
\item{ci_upper}{Upper confidence limit for d (non-central t)}
\item{correction}{The bias correction applied}
}
\description{
Calculates the standardized mean difference at each time point and channel,
with an exact non-central-t confidence interval (Cumming 2014) and an
optional Hedges small-sample bias correction.
}
\examples{
# Create example epoched data
set.seed(123)
epochs <- array(rnorm(100 * 4 * 20 * 1), dim = c(100, 4, 20, 1))
pe <- PhysioExperiment(
  assays = list(epoched = epochs),
  samplingRate = 100
)
# Effect size for one-sample
result <- effectSize(pe, condition1 = 1:10)
# Hedges g between conditions
result2 <- effectSize(pe, condition1 = 1:10, condition2 = 11:20,
                      correction = "hedges")
}
\references{
Cumming, G. (2014). The New Statistics. Psychol Sci 25(1):7-29.
Hedges, L.V. (1981). J Educ Stat 6(2):107-128.
}
\seealso{
\code{\link[=tTestEpochs]{tTestEpochs()}} for significance testing, \code{\link[=bootstrapCI]{bootstrapCI()}} for
bootstrap confidence intervals, \code{\link[=rankBiserial]{rankBiserial()}} and \code{\link[=cliffsDelta]{cliffsDelta()}} for
rank-based effect sizes.
}
