% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-functional-regression.R
\name{functionalRegression}
\alias{functionalRegression}
\title{Function-on-scalar regression (coefficient curves)}
\usage{
functionalRegression(curves, predictor, n_perm = 1000L, seed = NULL)
}
\arguments{
\item{curves}{An \verb{N x P} matrix: \code{N} observations (curves), \code{P} domain points
(e.g. \% gait cycle).}

\item{predictor}{A length-\code{N} vector, \verb{N x q} matrix, or data frame of scalar
predictors. An intercept is added automatically.}

\item{n_perm}{Permutations for the curve-wide (family-wise) p-value per
coefficient (default 1000); set 0 to skip.}

\item{seed}{Optional integer seed for the permutation test (reproducibility).}
}
\value{
a \code{fosr_result}: \code{coefficients} (\verb{k x P}, incl. intercept), \code{se},
\code{tval}, \code{p_pointwise} (\verb{k x P}), \code{p_global} (length \code{k}, family-wise), the
fitted curves and \code{terms}.
}
\description{
Models a set of response CURVES as a linear function of one or more scalar
predictors, giving a coefficient curve for each predictor (how the waveform
changes per unit predictor) with pointwise inference and a domain-wide
permutation test.
}
\examples{
set.seed(1)
P <- 101; t <- seq(0, 1, length.out = P)
speed <- runif(40, 0.8, 1.6)
curves <- outer(speed, rep(1, P)) * matrix(sin(2 * pi * t), 40, P, byrow = TRUE) +
  matrix(rnorm(40 * P, 0, 0.05), 40, P)
fit <- functionalRegression(curves, speed, n_perm = 200)
fit$p_global                       # speed coefficient curve is significant
}
\references{
Ramsay JO, Silverman BW (2005) Functional Data Analysis; Reiss PT,
et al. (2010) function-on-scalar regression.
}
\seealso{
\code{\link[=scalarOnFunctionRegression]{scalarOnFunctionRegression()}}, \code{\link[=spmRegression]{spmRegression()}}
}
