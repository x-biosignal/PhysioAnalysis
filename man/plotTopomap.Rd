% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis-topomap.R
\name{plotTopomap}
\alias{plotTopomap}
\title{Topographic Map Visualization}
\usage{
plotTopomap(
  x,
  values = NULL,
  time = NULL,
  channel_values = NULL,
  assay_name = NULL,
  resolution = 100L,
  contours = TRUE,
  head_shape = TRUE,
  electrodes = TRUE,
  palette = "RdBu",
  limits = NULL,
  title = NULL,
  interpolation = c("idw", "spline"),
  spline_stiffness = 4L,
  spline_terms = 50L,
  spline_regularization = 0
)
}
\arguments{
\item{x}{A PhysioExperiment object with electrode positions.}

\item{values}{Optional numeric vector of values to plot. If NULL, uses
values from the specified time point.}

\item{time}{Time point in seconds to extract values (if values is NULL).}

\item{channel_values}{Named vector of channel values (alternative to values).}

\item{assay_name}{Optional assay name. If NULL, uses the default assay.}

\item{resolution}{Grid resolution for interpolation. Default is 100.}

\item{contours}{Logical. If TRUE, adds contour lines. Default is TRUE}

\item{head_shape}{Logical. If TRUE, draws head outline. Default is TRUE.}

\item{electrodes}{Logical. If TRUE, shows electrode positions. Default is TRUE.}

\item{palette}{Color palette name or vector of colors.}

\item{limits}{Numeric vector of length 2 for color scale limits.}

\item{title}{Plot title. If NULL, auto-generated.}

\item{interpolation}{Interpolation method. \code{"idw"} preserves the Shepard
inverse-distance-weighted default; \code{"spline"} uses Perrin spherical
splines on an upper-hemisphere lift.}

\item{spline_stiffness}{Positive integer of at least 2 controlling the
spherical-spline kernel. Default is 4.}

\item{spline_terms}{Positive integer number of Legendre terms. Default is 50.}

\item{spline_regularization}{Non-negative diagonal regularization applied to
the electrode kernel. The default 0 gives an interpolating spline; positive
values improve conditioning but need not reproduce electrode values.}
}
\value{
A ggplot object.
}
\description{
Functions for plotting scalp topography maps showing the spatial
distribution of signal values across electrode positions.
Plot topographic map (scalp topography)
}
\details{
Creates a 2D topographic map showing the spatial distribution of values
across electrode positions on the scalp.

With \code{interpolation = "idw"}, scalp values are interpolated by
inverse distance weighting (Shepard's method) with power 2. With
\code{interpolation = "spline"}, planar montage coordinates are lifted to a
shared upper unit hemisphere and evaluated with the Perrin spherical-spline
kernel. This is spatial interpolation, not a surface Laplacian,
current-source-density estimate, reference transformation, or source
localization.
}
\examples{
# Create example with 10-20 electrode positions
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(400), nrow = 100, ncol = 4)),
  colData = S4Vectors::DataFrame(label = c("Fz", "Cz", "Pz", "Oz")),
  samplingRate = 100
)

# Apply 10-20 montage to get electrode positions
pe <- applyMontage(pe, "10-20")

# Plot topographic map at time = 0.5s
plotTopomap(pe, time = 0.5)

# Plot with custom values
plotTopomap(pe, values = c(1, 0.5, -0.5, -1))
}
\references{
Shepard, D. (1968). "A two-dimensional interpolation function
for irregularly-spaced data." \emph{Proceedings of the 1968 23rd ACM
National Conference}, 517-524. \doi{10.1145/800186.810616}

Perrin F, Pernier J, Bertrand O, Echallier J. (1989). Spherical splines
for scalp potential and current density mapping.
\emph{Electroencephalography and Clinical Neurophysiology}, 72(2),
184-187. \doi{10.1016/0013-4694(89)90180-6}
}
\seealso{
\code{\link[=plotTopomapSeries]{plotTopomapSeries()}} for topographic maps across time,
\code{\link[=plotMultiChannel]{plotMultiChannel()}} for multi-channel signal visualization,
\code{\link[=plotERP]{plotERP()}} for event-related potential plots.
}
