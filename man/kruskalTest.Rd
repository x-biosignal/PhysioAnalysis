% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{kruskalTest}
\alias{kruskalTest}
\title{Kruskal-Wallis rank-sum test (non-parametric between-groups omnibus)}
\usage{
kruskalTest(x, ...)
}
\arguments{
\item{x}{Either a list of \code{k >= 2} numeric group vectors, or the first
group's numeric vector (with further groups passed through \code{...}).}

\item{...}{Additional numeric group vectors when \code{x} is a single vector.}
}
\value{
A list with \code{statistic} (the tie-corrected H), \code{p_value}, \code{df}
(\code{k - 1}), \code{k} (number of groups) and \code{n} (total sample size).
}
\description{
The non-parametric between-groups omnibus test: pools all observations,
ranks them, and tests whether the rank totals differ across \code{k >= 2}
independent groups. It is the distribution-free counterpart of the one-way
ANOVA (\code{\link{oneWayAnova}}) and, unlike it, makes no normality or
equal-variance assumption. The tie-corrected statistic and its p-value
reproduce base R \code{stats::kruskal.test} bit-for-bit (the H is accumulated in
the same order) and \code{scipy.stats.kruskal} to machine precision.
}
\examples{
kruskalTest(list(rnorm(20), rnorm(20, 1), rnorm(20, 2)))
}
\seealso{
\code{\link{oneWayAnova}}, \code{\link{friedmanTest}}
}
