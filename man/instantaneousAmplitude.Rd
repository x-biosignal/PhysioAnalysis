% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{instantaneousAmplitude}
\alias{instantaneousAmplitude}
\title{Extract instantaneous amplitude (envelope)}
\usage{
instantaneousAmplitude(x, assay_name = "analytic", output_assay = "amplitude")
}
\arguments{
\item{x}{A PhysioExperiment object with analytic signal.}

\item{assay_name}{Name of the analytic signal assay.}

\item{output_assay}{Name for the output assay.}
}
\value{
A \code{PhysioExperiment} object with an additional assay (default
\code{"amplitude"}) containing the real-valued instantaneous amplitude
(envelope) of the signal.
}
\description{
Extracts the instantaneous amplitude (envelope) from the analytic signal.
}
\examples{
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(500 * 4), nrow = 500)),
  samplingRate = 100
)

# First compute Hilbert transform, then extract amplitude
pe <- hilbertTransform(pe)
pe <- instantaneousAmplitude(pe)
}
\references{
Oppenheim, A.V. & Willsky, A.S. (1997). "Signals and Systems."
2nd ed. Prentice Hall.
}
\seealso{
\code{\link[=hilbertTransform]{hilbertTransform()}} which must be called first,
\code{\link[=instantaneousPhase]{instantaneousPhase()}} for the companion phase extraction,
\code{\link[=waveletTransform]{waveletTransform()}} for alternative time-frequency decomposition.
}
