% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{kendallTau}
\alias{kendallTau}
\title{Kendall's tau-b rank correlation}
\usage{
kendallTau(x, y = NULL)
}
\arguments{
\item{x}{Either the numeric predictor vector (with \code{y} the response), or a
two-column matrix/data frame (column 1 = \code{x}, column 2 = \code{y}), or a list of
two numeric vectors. Incomplete \verb{(x, y)} pairs are dropped.}

\item{y}{The second numeric vector when \code{x} is a single vector.}
}
\value{
A list with \code{statistic} (tau-b), \code{p_value} (two-sided,
normal-approximation), \code{z} (the approximation's z-statistic) and \code{n}.
}
\description{
Kendall's tau-b, the concordance-based rank correlation: the normalised
difference between concordant and discordant pairs, tie-corrected. It is the
third rank-correlation method alongside Pearson's r
(\code{\link{correlationTest}}, method "pearson") and Spearman's rho (method
"spearman"), and is the robust choice for ordinal or heavily-tied data and
small samples. The tau-b statistic and the normal-approximation p-value
reproduce base R \code{stats::cor.test(method = "kendall")} and
\code{scipy.stats.kendalltau} bit-for-bit.
}
\examples{
kendallTau(c(1, 2, 3, 4, 5), c(2, 1, 4, 3, 5))
}
\seealso{
\code{\link{correlationTest}}
}
