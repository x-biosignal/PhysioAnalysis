% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-spm-nonparam.R
\name{spmSnPM}
\alias{spmSnPM}
\title{Non-parametric (permutation) SPM}
\usage{
spmSnPM(
  x,
  groups = NULL,
  group1 = NULL,
  group2 = NULL,
  statistic = c("t", "F", "T2"),
  n_permutations = 1000,
  alpha = 0.05,
  two_tailed = TRUE,
  vector_components = NULL,
  seed = NULL
)
}
\arguments{
\item{x}{A \code{PhysioExperiment}/matrix (time x obs) for \code{t}/\code{F},
or a 3D array / list of component matrices for \code{T2}.}

\item{groups}{Grouping factor (one per observation) for \code{F} or \code{T2},
or a two-sample \code{t}.}

\item{group1, group2}{Observation indices for a two-sample \code{t}
(\code{group2 = NULL} gives a one-sample sign-flip test).}

\item{statistic}{\code{"t"}, \code{"F"}, or \code{"T2"}.}

\item{n_permutations}{Number of random permutations (default 1000).}

\item{alpha}{Significance level (default 0.05).}

\item{two_tailed}{For \code{t}: use the two-tailed field maximum \code{max|t|}.}

\item{vector_components}{Passed to the \code{T2} array coercion.}

\item{seed}{Optional RNG seed for reproducibility.}
}
\value{
A list of class \code{"spm_result"} (\code{test_type = "snpm"}) with
the observed statistic field, the permutation \code{threshold},
\code{clusters} with permutation p-values, pointwise permutation
\code{p_values}, and the \code{perm_max} distribution.
}
\description{
Builds the permutation distribution of the field-maximum statistic to obtain a
distribution-free critical threshold with strong FWER control (Nichols &
Holmes 2002), plus permutation cluster- and set-level p-values. This is the
validity cross-check for the parametric RFT thresholds: as the field smoothness
grows the permutation threshold converges to the RFT threshold.
}
\examples{
set.seed(1)
g1 <- matrix(rnorm(50 * 10), 50); g2 <- matrix(rnorm(50 * 10), 50)
g2[20:30, ] <- g2[20:30, ] + 1.5
spmSnPM(cbind(g1, g2), group1 = 1:10, group2 = 11:20,
        n_permutations = 200, seed = 1)
}
\references{
Nichols & Holmes 2002; Pataky 2016. spm1d.stats.nonparam.
}
\seealso{
\code{\link[=spmTTest]{spmTTest()}}, \code{\link[=spmAnova]{spmAnova()}}, \code{\link[=spmMANOVA]{spmMANOVA()}}
}
