% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{friedmanTest}
\alias{friedmanTest}
\title{Friedman rank-sum test (non-parametric repeated-measures omnibus)}
\usage{
friedmanTest(x, ...)
}
\arguments{
\item{x}{A blocks-by-treatments numeric matrix (rows = subjects/blocks,
columns = conditions/treatments), or a list of \code{k} equal-length numeric
vectors (one per treatment, aligned by block). Rows with any missing value
are dropped (complete blocks only).}

\item{...}{Additional treatment vectors when \code{x} is a single vector.}
}
\value{
A list with \code{statistic} (the tie-corrected Friedman chi-square),
\code{p_value}, \code{df} (\code{k - 1}), \code{k} (treatments) and \code{n} (complete blocks).
}
\description{
The non-parametric repeated-measures omnibus test: compares \code{k >= 2} matched
treatments (columns) measured on the same \code{N} blocks/subjects (rows), by
ranking within each block and testing whether the rank totals differ. It is
the within-subject, distribution-free counterpart of the one-way ANOVA
(\code{\link{oneWayAnova}}). The tie-corrected chi-square statistic and its
p-value reproduce base R \code{stats::friedman.test} bit-for-bit and
\code{scipy.stats.friedmanchisquare} to machine precision.
}
\examples{
m <- matrix(rnorm(40) + rep(c(0, 0.5, 1, 1.5), each = 10), nrow = 10)
friedmanTest(m)
}
\seealso{
\code{\link{oneWayAnova}}, \code{\link{pairedTTest}}
}
