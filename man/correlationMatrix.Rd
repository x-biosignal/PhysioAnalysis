% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-connectivity.R
\name{correlationMatrix}
\alias{correlationMatrix}
\title{Compute correlation matrix between channels}
\usage{
correlationMatrix(
  x,
  channels = NULL,
  method = c("pearson", "spearman", "kendall"),
  assay_name = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{channels}{Integer vector of channel indices.}

\item{method}{Correlation method: "pearson", "spearman", or "kendall".}

\item{assay_name}{Input assay name.}
}
\value{
A numeric correlation matrix (channel x channel) with values
ranging from -1 to 1. Row and column names are set to channel names
when available.
}
\description{
Calculates the Pearson correlation coefficient between all channel pairs.
}
\examples{
set.seed(123)
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(4000), nrow = 1000, ncol = 4)),
  colData = S4Vectors::DataFrame(label = c("Fz", "Cz", "Pz", "Oz")),
  samplingRate = 256
)

# Compute Pearson correlation
cor_matrix <- correlationMatrix(pe)
}
\references{
Oppenheim, A.V. & Willsky, A.S. (1997). "Signals and Systems."
2nd ed. Prentice Hall.
}
\seealso{
\code{\link[=coherence]{coherence()}} for frequency-domain coherence, \code{\link[=plv]{plv()}} for
phase-based connectivity, \code{\link[=connectivityMatrix]{connectivityMatrix()}} for a unified
connectivity interface.
}
