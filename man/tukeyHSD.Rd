% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{tukeyHSD}
\alias{tukeyHSD}
\title{Tukey HSD post-hoc test (all pairwise comparisons after one-way ANOVA)}
\usage{
tukeyHSD(x, ..., conf_level = 0.95)
}
\arguments{
\item{x}{Either a named list of \code{k >= 2} numeric group vectors, or the first
group's numeric vector (with further groups passed through \code{...} -- use a
named list to label the comparisons).}

\item{...}{Additional numeric group vectors when \code{x} is a single vector.}

\item{conf_level}{Confidence level for the intervals (default 0.95).}
}
\value{
A data frame with one row per pair: \code{comparison}, \code{diff} (mean
difference), \code{lwr}/\code{upr} (CI) and \code{p_adj} (family-wise-adjusted p-value),
carrying \code{k}, \code{df} and \code{n} as attributes.
}
\description{
Tukey's Honest Significant Difference test: all pairwise mean comparisons
following a one-way ANOVA (\code{\link{oneWayAnova}}), with p-values and
confidence intervals from the studentized-range distribution and single-step
control of the family-wise error rate. Reproduces base R
\code{stats::TukeyHSD} and \code{scipy.stats.tukey_hsd} (machine precision).
}
\examples{
tukeyHSD(list(a = rnorm(20), b = rnorm(20, 1), c = rnorm(20, 2)))
}
\seealso{
\code{\link{oneWayAnova}}
}
