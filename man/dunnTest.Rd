% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{dunnTest}
\alias{dunnTest}
\title{Dunn's test (post-hoc pairwise comparisons after Kruskal-Wallis)}
\usage{
dunnTest(x, ..., p_adjust = c("bonferroni", "none", "holm", "BH"))
}
\arguments{
\item{x}{Either a named list of \code{k >= 2} numeric group vectors, or the first
group's numeric vector (with further groups passed through \code{...}).}

\item{...}{Additional numeric group vectors when \code{x} is a single vector.}

\item{p_adjust}{Multiple-comparison adjustment: "bonferroni" (default),
"none", "holm" or "BH".}
}
\value{
A data frame with one row per pair: \code{comparison}, \code{z},
\code{p_value} (two-sided, unadjusted) and \code{p_adj}, carrying \code{k}, \code{n} as attributes.
}
\description{
Dunn's test: all pairwise rank-based comparisons following a Kruskal-Wallis
test (\code{\link{kruskalTest}}), using the shared tie-corrected rank variance
and a chosen multiple-comparison adjustment. It is the non-parametric
counterpart of Tukey's HSD (\code{\link{tukeyHSD}}). The z-statistics and
adjusted p-values reproduce base R's Dunn formula, the \code{PMCMRplus} package and
\code{scikit_posthocs.posthoc_dunn} to machine precision.
}
\examples{
dunnTest(list(a = rnorm(20), b = rnorm(20, 1), c = rnorm(20, 2)))
}
\seealso{
\code{\link{kruskalTest}}, \code{\link{tukeyHSD}}
}
