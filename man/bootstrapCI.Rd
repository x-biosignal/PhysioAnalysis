% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-tests.R
\name{bootstrapCI}
\alias{bootstrapCI}
\title{Bootstrap confidence interval for ERP}
\usage{
bootstrapCI(
  x,
  n_bootstrap = 1000L,
  ci_level = 0.95,
  condition = NULL,
  seed = NULL
)
}
\arguments{
\item{x}{An epoched PhysioExperiment object (4D data).}

\item{n_bootstrap}{Number of bootstrap iterations (default: 1000).}

\item{ci_level}{Confidence interval level (default: 0.95).}

\item{condition}{Epoch indices to include. If NULL, uses all epochs.}

\item{seed}{Random seed for reproducibility.}
}
\value{
A list containing:
\item{mean}{Mean across epochs (time x channel)}
\item{ci_lower}{Lower CI bound (time x channel)}
\item{ci_upper}{Upper CI bound (time x channel)}
\item{se}{Standard error (time x channel)}
}
\description{
Computes bootstrap confidence intervals for averaged epochs.
}
\examples{
# Create example epoched data
set.seed(123)
epochs <- array(rnorm(100 * 4 * 20 * 1), dim = c(100, 4, 20, 1))
pe <- PhysioExperiment(
  assays = list(epoched = epochs),
  samplingRate = 100
)
# Bootstrap CI
result <- bootstrapCI(pe, n_bootstrap = 500)
}
\references{
Maris, E. & Oostenveld, R. (2007). "Nonparametric statistical
testing of EEG- and MEG-data." \emph{Journal of Neuroscience Methods},
164(1), 177-190. \doi{10.1016/j.jneumeth.2007.03.024}
}
\seealso{
\code{\link[=effectSize]{effectSize()}} for Cohen's d effect size, \code{\link[=tTestEpochs]{tTestEpochs()}} for
parametric significance testing, \code{\link[=plotERP]{plotERP()}} for ERP visualization.
}
