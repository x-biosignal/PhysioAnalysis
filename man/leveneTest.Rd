% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{leveneTest}
\alias{leveneTest}
\title{Levene's test for homogeneity of variance}
\usage{
leveneTest(x, ..., center = c("median", "mean"))
}
\arguments{
\item{x}{Either a list of \code{k >= 2} numeric group vectors, or the first
group's numeric vector (with further groups passed through \code{...}).}

\item{...}{Additional numeric group vectors when \code{x} is a single vector.}

\item{center}{"median" (default, Brown-Forsythe) or "mean" (classic Levene).}
}
\value{
A list with \code{statistic} (F), \code{p_value}, \code{df1} (\code{k - 1}), \code{df2}
(\code{N - k}), \code{k}, \code{n} and \code{center}.
}
\description{
Levene's test of whether \code{k >= 2} groups have equal variance, as a one-way
ANOVA on the absolute deviations from each group's centre. With
\code{center = "median"} (default) it is the robust Brown-Forsythe variant,
appropriate when the data may be non-normal; \code{center = "mean"} is the classic
Levene test. The F-statistic and p reproduce \code{scipy.stats.levene} and the
\code{car} package (\code{car::leveneTest}) bit-for-bit.
}
\examples{
leveneTest(list(rnorm(20), rnorm(20, 0, 2), rnorm(20, 0, 0.5)))
}
\seealso{
\code{\link{oneWayAnova}}
}
