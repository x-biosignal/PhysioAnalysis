% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{oneWayAnova}
\alias{oneWayAnova}
\title{One-way ANOVA (omnibus F-test) across groups}
\usage{
oneWayAnova(x, ...)
}
\arguments{
\item{x}{Either a list of \code{k >= 2} numeric group vectors, or the first
group's numeric vector (with further groups passed through \code{...}).}

\item{...}{Additional numeric group vectors when \code{x} is a single vector.}
}
\value{
A list with \code{statistic} (the F-ratio), \code{p_value}, \code{df1}
(between-groups degrees of freedom, \code{k - 1}), \code{df2} (within-groups,
\code{N - k}), \code{k} (number of groups) and \code{n} (total sample size).
}
\description{
Classic one-way analysis of variance (equal-variance omnibus F-test)
comparing the means of \code{k >= 2} independent groups. The F-statistic and its
p-value reproduce base R \code{stats::oneway.test(var.equal = TRUE)} and
\code{scipy.stats.f_oneway} bit-for-bit. This is the standard many-group
generalisation of the independent two-sample t-test
(\code{\link{twoSampleTTest}}).
}
\examples{
oneWayAnova(list(rnorm(20), rnorm(20, 1), rnorm(20, 2)))
}
\seealso{
\code{\link{twoSampleTTest}}, \code{\link{pairedTTest}}
}
