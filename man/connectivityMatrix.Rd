% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-connectivity.R
\name{connectivityMatrix}
\alias{connectivityMatrix}
\title{Compute connectivity matrix for a frequency band}
\usage{
connectivityMatrix(
  x,
  method = c("coherence", "plv", "pli", "wpli", "correlation"),
  freq_band = NULL,
  channels = NULL,
  assay_name = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{method}{Connectivity method: "coherence", "plv", "pli", "wpli", "correlation".}

\item{freq_band}{Frequency band for phase-based methods.}

\item{channels}{Integer vector of channel indices.}

\item{assay_name}{Input assay name.}
}
\value{
A numeric matrix (channel x channel) of connectivity values.
For \code{"coherence"}, returns the mean coherence across frequencies.
For phase-based methods (\code{"plv"}, \code{"pli"}, \code{"wpli"}), returns
values between 0 and 1. For \code{"correlation"}, returns values between
-1 and 1.
}
\description{
High-level function to compute connectivity using various metrics.
}
\examples{
set.seed(123)
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(4000), nrow = 1000, ncol = 4)),
  samplingRate = 256
)

# Compute PLV connectivity in alpha band
conn <- connectivityMatrix(pe, method = "plv", freq_band = c(8, 12))
}
\references{
Lachaux, J.-P., et al. (1999). "Measuring phase synchrony in
brain signals." Human Brain Mapping, 8(4), 194-208.
doi:10.1002/(SICI)1097-0193(1999)8:4<194::AID-HBM4>3.0.CO;2-C
}
\seealso{
\code{\link[=coherence]{coherence()}}, \code{\link[=plv]{plv()}}, \code{\link[=pli]{pli()}}, \code{\link[=wPLI]{wPLI()}},
\code{\link[=correlationMatrix]{correlationMatrix()}} for individual connectivity methods.
}
